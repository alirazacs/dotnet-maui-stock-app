<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sf="clr-namespace:Syncfusion.Maui.ListView;assembly=Syncfusion.Maui.ListView"
             xmlns:local="clr-namespace:DotnetTrainingStockApp"
             x:Class="DotnetTrainingStockApp.ListView">
    <!---Binding page to items view model-->
    <ContentPage.BindingContext>
        <local:ItemsModel></local:ItemsModel>
    </ContentPage.BindingContext>
    <sf:SfListView ItemsSource="{Binding ItemsList}">

        <sf:SfListView.HeaderTemplate>
            <DataTemplate>
                <Grid BackgroundColor="Aqua" HeightRequest="45">
                    <Label Text="Your Cart"
                           FontAttributes="Bold"
                           HorizontalTextAlignment="Center"
                           VerticalTextAlignment="Center"
                           TextColor="White"/>
                </Grid>
            </DataTemplate>
        </sf:SfListView.HeaderTemplate>

        <sf:SfListView.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Item Name Label -->
                    <Label Text="{Binding ItemName, StringFormat='{}Name: {0}'}" TextColor="CadetBlue"/>

                    <!-- Nested Grid for Labels and Button -->
                    <Grid Grid.Row="1" RowSpacing="2" VerticalOptions="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>


                        <Label Text="{Binding ItemPrice,StringFormat='{}Price: {0}'} " Grid.Column="0" />


                        <Label Text="{Binding Quanity,StringFormat='{}Quantity: {0}'}" Grid.Column="1"/>

                        <Label Text="{Binding TotalAmount,StringFormat='{}Total: {0}'}" Grid.Column="2" />

                        <Button Text="View Details" Grid.Column="4"  HorizontalOptions="Center"/>

                    </Grid>
                </Grid>
            </DataTemplate>
        </sf:SfListView.ItemTemplate>
    </sf:SfListView>
</ContentPage>